import { baseUrl } from "../handleApi";

export const urls = {
    users: `${baseUrl}/user-lao`,
    update_user: `${baseUrl}/update-user`,
    create: `${baseUrl}//create-user`,
    delete: `${baseUrl}/delete-user`,
    find_user: `${baseUrl}/find-user`,
  };
  
  export default urls;